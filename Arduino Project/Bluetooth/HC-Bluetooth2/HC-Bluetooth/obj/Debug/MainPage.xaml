<Page
    x:Class="HC_Bluetooth.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HC_Bluetooth"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="#FF1C4C64">
        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="12,0,12,538">
            <TextBlock x:Name="txbTitile" Text="ĐIỀU KHIỂN THIẾT BỊ" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0" FontSize="36"/>
            <TextBlock x:Name="txbStatus" Text="Vui lòng kết nối đến thiết bị." Margin="0" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}" FontSize="20"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <Grid x:Name="ContentRoot" Margin="12,128,12,0">
            <StackPanel Orientation="Vertical" Margin="0">
                <TextBox x:Name="messageTextBlock" Height="290" AcceptsReturn="True" Margin="0,0,-0.333,0"/>
                <Button x:ConnectionId='1' Content="Send" x:Name="btnSend" IsEnabled="false" HorizontalAlignment="Stretch"                       Foreground="White" Margin="0,0,-0.167,0"/>
                <TextBlock x:Name="txbConnectStatus" Text="Starting...." Margin="0,0,-0.167,0"/>
                <Button x:ConnectionId='2' x:Name="btnConnect" Content="Connect"  IsEnabled="false" HorizontalAlignment="Stretch"                          Foreground="White" Margin="0,0,-0.167,0"/>
                <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="© 2016 HC-Bluetooth.All right reserved." TextAlignment="Center" VerticalAlignment="Bottom" Margin="0,65,-0.167,0" FontSize="14"/>
            </StackPanel>
        </Grid>

    </Grid>
</Page>

